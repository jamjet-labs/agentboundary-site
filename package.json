{
  "name": "agentboundary-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Microsite for the AgentBoundary open spec. Deploys to agentboundary.jamjet.dev.",
  "scripts": {
    "prebuild": "node scripts/sync-spec.mjs && node scripts/verify-schema-identity.mjs",
    "dev": "node scripts/sync-spec.mjs && astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^5.3.0",
    "@astrojs/sitemap": "^3.5.0",
    "rehype-autolink-headings": "^7.1.0",
    "rehype-slug": "^6.0.0",
    "remark-gfm": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.4.0"
  },
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=20"
  }
}
